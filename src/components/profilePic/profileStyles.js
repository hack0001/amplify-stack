const styles = theme => ({
  root: {
    backgroundColor: theme.palette.background.paper
  },
  textField: {
    width: 500,
    margin: 20
  },
  delete: {
    float: "right"
  }
});

export { styles };
